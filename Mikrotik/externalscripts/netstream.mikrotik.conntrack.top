#!/bin/bash
# netstream.mikrotik.conntrack.top
# Wrapper para conntrack.top.mikrotik.py
# Chave Zabbix: netstream.mikrotik.conntrack.top[{HOST.IP},{$MIKROTIK_SSH_USER},{$MIKROTIK_SSH_PASS},{$CONNTRACK_TOP_N}]

SCRIPT_DIR="$(dirname "$0")"
exec python3 "$SCRIPT_DIR/conntrack.top.mikrotik.py" "$@"
